const x = 12; 
const y = 3;

//addition
console.log(x + y); // hear x and y are operands + is operater 
// -o/p = 15
/**
 * if the assiging value is in sting ex: let x = "12"; and let y = "3"; 
 * -o/p = 123
 * if there is any one value is in string the other will converter into string
 * the  -o/p = 123
 * if let x = "mango"; and let y = "apple";
 *  -o/p = mangoapple
 *
 */


//subtraction
console.log(x - y);  // hear x and y are operands - is operater
// -o/p = 9
/**
 * if the assiging value is in string ex: let x = "12"; and let y = "3"; 
 * -o/p = 9 beacuse the string will be converted into integer
 * if there is any one value is in string the other will converter into integer
 * the  -o/p = 9
 * if let x = "mango"; and let y = "apple";
 *  -o/p = NaN (Not a Number)
 */


//multiplication
console.log(x * y); // hear x and y are operands * is operater
// -o/p = 36
/**
 * if the assiging value is in string ex: let x = "12"; and let y = "3"; 
 * -o/p = 9 beacuse the string will be converted into integer
 * if there is any one value is in string the other will converter into integer
 * the  -o/p = 9
 * if let x = "mango"; and let y = "apple";
 *  -o/p = NaN (Not a Number)
 */


//division
console.log(x / y); // hear x and y are operands / is operater
// -o/p = 4
/**
 * if the assiging value is in string ex: let x = "12"; and let y = "3"; 
 * -o/p = 4 beacuse the string will be converted into integer
 * if there is any one value is in string the other will converter into integer
 * the  -o/p = 4
 * if let x = "mango"; and let y = "apple";
 *  -o/p = NaN (Not a Number)
 */


//modulo division to find the reminder
console.log(x % y); // hear x and y are operands % is operater
// -o/p = 0
/**
 * if the assiging value is in string ex: let x = "12"; and let y = "3"; 
 * -o/p = 0 beacuse the string will be converted into integer
 * if there is any one value is in string the other will converter into integer
 * the  -o/p = 0
 * if let x = "mango"; and let y = "apple";
 *  -o/p = NaN (Not a Number)
 */


//power square
console.log(x ** 2); // hear x and y are operands ** is operater
// -o/p = 144
/**
 * if the assiging value is in string ex: let x = "12"; and let y = "3"; 
 * -o/p = 144 beacuse the string will be converted into integer
 * if there is any one value is in string the other will converter into integer
 * the  -o/p = 144
 * if let x = "mango"; and let y = "apple";
 *  -o/p = NaN (Not a Number)
 */


//cube
console.log(y ** 3);
// -o/p = 27
/**
 * if the assiging value is in string ex: let x = "12"; and let y = "3"; 
 * -o/p = 27 beacuse the string will be converted into integer
 * if there is any one value is in string the other will converter into integer
 * the  -o/p = 27
 * if let x = "mango"; and let y = "apple";
 *  -o/p = NaN (Not a Number)
 */
