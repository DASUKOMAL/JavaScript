/** const number;  
 * 'const' declarations must be initialized. */
const number =10;
/** number = 4;
 * we cannot modify in const variable
 * if something in constent that cannat be changed */
console.log(number);
